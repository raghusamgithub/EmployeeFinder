<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>XML Mapper with Values</title>
    <script src="https://cdn.sheetjs.com/xlsx-0.20.1/package/dist/xlsx.full.min.js"></script>
    <style>
        body { font-family: 'Segoe UI', sans-serif; background-color: #f8f9fa; padding: 40px; color: #333; }
        .container { max-width: 1200px; margin: auto; background: white; padding: 30px; border-radius: 12px; box-shadow: 0 4px 15px rgba(0,0,0,0.1); }
        .upload-section { text-align: center; border-bottom: 2px solid #eee; padding-bottom: 30px; margin-bottom: 30px; }
        .upload-btn { background: #007bff; color: white; padding: 12px 25px; border-radius: 6px; cursor: pointer; display: inline-block; font-weight: bold; }
        #fileInput { display: none; }
        
        .preview-container { overflow-x: auto; margin-top: 20px; }
        table { border-collapse: collapse; width: 100%; font-size: 13px; font-family: 'Courier New', Courier, monospace; }
        th { background: #e9ecef; text-align: left; padding: 10px; border: 1px solid #dee2e6; }
        td { border: 1px solid #dee2e6; padding: 8px 15px; min-width: 120px; }
        .node-text { font-weight: 600; color: #0056b3; }
        .value-text { color: #28a745; font-weight: 400; }
        .empty-cell { background-color: #ffffff; }
        #status { margin-top: 15px; font-weight: bold; }
    </style>
</head>
<body>

<div class="container">
    <div class="upload-section">
        <h2>SOAP Hierarchy Mapper</h2>
        <p>Extracts structure and <b>values</b> into a clean Excel report.</p>
        <label for="fileInput" class="upload-btn">Upload XML File</label>
        <input type="file" id="fileInput" accept=".xml" onchange="processXML()">
        <div id="status"></div>
    </div>

    <div class="preview-container">
        <h3>Cleaned Structure & Values Preview</h3>
        <table id="previewTable"></table>
    </div>
</div>

<script>
    function processXML() {
        const fileInput = document.getElementById('fileInput');
        const status = document.getElementById('status');
        const table = document.getElementById('previewTable');
        const file = fileInput.files[0];

        if (!file) return;

        const reader = new FileReader();
        status.innerHTML = "Processing...";
        table.innerHTML = ""; 

        reader.onload = function(e) {
            try {
                const xmlText = e.target.result;
                const parser = new DOMParser();
                const xmlDoc = parser.parseFromString(xmlText, "text/xml");

                if (xmlDoc.getElementsByTagName("parsererror").length > 0) {
                    throw new Error("Invalid XML file format.");
                }

                const rows = [];
                let maxDepth = 0;

                function traverse(node, depth) {
                    if (node.nodeType === 1) { // Element Node
                        if (depth > maxDepth) maxDepth = depth;
                        
                        // Get direct text content (if it's a leaf node)
                        let value = "";
                        if (node.childNodes.length === 1 && node.firstChild.nodeType === 3) {
                            value = node.textContent.trim();
                        }
                        
                        rows.push({
                            depth: depth,
                            name: node.localName,
                            value: value
                        });

                        let child = node.firstChild;
                        while (child) {
                            traverse(child, depth + 1);
                            child = child.nextSibling;
                        }
                    }
                }

                traverse(xmlDoc.documentElement, 0);

                // Prepare Data for Excel/Table (Structure columns + 1 Value column)
                const finalData = rows.map(r => {
                    let arr = new Array(maxDepth + 2).fill("");
                    arr[r.depth] = r.name;
                    arr[maxDepth + 1] = r.value; // Always place value in the last column
                    return arr;
                });

                // 1. Generate Excel
                const worksheet = XLSX.utils.aoa_to_sheet(finalData);
                const workbook = XLSX.utils.book_new();
                XLSX.utils.book_append_sheet(workbook, worksheet, "XML Structure");
                XLSX.writeFile(workbook, "xml_response_structure.xlsx");

                // 2. Display Preview
                // Create Header
                let thead = "<thead><tr>";
                for(let i=0; i<=maxDepth; i++) thead += `<th>Level ${i}</th>`;
                thead += "<th>Value</th></tr></thead>";
                table.innerHTML = thead;

                const tbody = document.createElement('tbody');
                finalData.forEach(rowData => {
                    const tr = document.createElement('tr');
                    rowData.forEach((cellData, index) => {
                        const td = document.createElement('td');
                        const isValueCol = (index === maxDepth + 1);
                        
                        if (cellData) {
                            td.innerHTML = `<span class="${isValueCol ? 'value-text' : 'node-text'}">${cellData}</span>`;
                        } else {
                            td.className = "empty-cell";
                        }
                        tr.appendChild(td);
                    });
                    tbody.appendChild(tr);
                });
                table.appendChild(tbody);

                status.innerHTML = "<span style='color:green;'>✅ Success! Processed " + rows.length + " nodes.</span>";
            } catch (err) {
                status.innerHTML = "<span style='color:red;'>Error: " + err.message + "</span>";
            }
        };

        reader.readAsText(file);
    }
</script>

</body>
</html>
